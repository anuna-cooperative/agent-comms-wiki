# Algorithm = Logic + Control

Kowalski's dictum that an algorithm can be factored into a *logic* component (what is known) and a *control* component (how inferences are sequenced); logic programming aims to let programmers specify the former while the system handles the latter.

## In this vault
- [[Foundations of Logic Programming - Lloyd]]
